# 题目3：质因数分解（数论）

给定一个正整数 N（2 ≤ N ≤ 10^12），输出其质因数分解结果。

**输入**
一个整数 N。

**输出**
```
N = p1^k1 * p2^k2 * ... * pm^km
```
其中 p1 < p2 < ... < pm，ki ≥ 1。若 ki = 1 则省略 ^ki。

**样例**
```
100
```
```
100 = 2^2 * 5^2
```

**要求：** 用 sqrt(N) 试除，文件名 `factorize.c`
