# 题目7：前缀和与差分（区间加）

长度为 N 的数组初始全 0。进行 M 次操作，每次 `l r x` 表示对 [l, r] 区间每个数加 x。最后输出整个数组。

**输入**
第一行 N, M（1 ≤ N, M ≤ 10^5）
接下来 M 行，每行三个整数 l, r, x（1 ≤ l ≤ r ≤ N, |x| ≤ 1000）

**输出**
一行 N 个整数，用空格隔开。

**样例**
```
5 3
1 3 2
2 5 -1
3 4 5
```
```
2 1 6 4 -1
```

**要求：** 用差分数组 O(N+M)，文件名 `diff_array.c`
