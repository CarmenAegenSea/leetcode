# 题目10：逆序对（分治/归并排序）

给定长度为 N 的序列，求其中逆序对的数量。
逆序对：i < j 且 a[i] > a[j]。

**输入**
第一行 N（1 ≤ N ≤ 10^5）
第二行 N 个整数，绝对值 ≤ 10^9

**输出**
逆序对个数。答案可能超过 int 范围。

**样例**
```
6
5 4 2 6 3 1
```
```
11
```

**要求：** 归并排序 O(N log N)，文件名 `inversion.c`
