# 题目8：字符串匹配（KMP）

给定文本串 T 和模式串 P，求 P 在 T 中**所有出现**的首位置下标（从1开始）。

**输入**
第一行 T（长度 ≤ 10^6）
第二行 P（长度 ≤ 10^5）

**输出**
每行一个整数，若没有输出空。

**样例**
```
ababcabcabababd
ababd
```
```
11
```

**要求：** 用 KMP 算法 O(N+M)，文件名 `kmp.c`
