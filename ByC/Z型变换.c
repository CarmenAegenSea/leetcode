#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char* convert(char* s, int numRows) {
    int length = strlen(s);
    if (numsRows == 1 || numRows >= length) {
        return s;
    }

    char* returnS = (char*)calloc(length, ' ');
    int T = 2 * (numRows - 1);
    for (int i = 0; i < numRows; i++) {`
        for (int j = 0; j < length; j++) {
            
        }
    }
}